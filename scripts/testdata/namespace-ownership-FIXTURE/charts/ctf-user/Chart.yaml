# Fixture (Issue #198, follow-up to platform#75 / ADR-0011; originally added
# for the 5x R3 follow-up to platform#111 when ctf-user was still excluded):
# a chart directory named exactly "ctf-user" that self-templates a
# Namespace, used to prove scripts/check-namespace-ownership.sh's
# (now-removed) by-name exclusion still applied under --charts-dir. Issue
# #198 lifted that exclusion, so this fixture now proves the opposite: the
# guard must FIRE on a chart named "ctf-user" exactly like any other chart
# (bad-chart/list-chart), with no residual by-name exemption. Used only by
# the namespace-ownership-guard-negative CI job. Not the real
# charts/ctf-user chart — do not use as an example of that chart's
# structure.
apiVersion: v2
name: ctf-user
description: Fixture chart named ctf-user that self-templates a Namespace (Issue #198: must FAIL like any other chart, no exclusion)
type: application
version: 0.1.0
appVersion: "0.0.0"
