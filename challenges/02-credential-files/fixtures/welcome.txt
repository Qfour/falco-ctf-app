┌──────────────────────────────────────────────────────────────────┐
│ CTF Company · Mission 02 — Credential Files                      │
│ Type: TRIGGER · 難易度: ★☆☆☆☆                                    │
│ 目標 Falco ルール: "Read sensitive file untrusted"                │
└──────────────────────────────────────────────────────────────────┘

# シナリオ

Mission 01 で「書き込めば即バレる」と分かった。次は王道の credential
窃取だ。Linux で password hash が眠っている場所と言えば `/etc/shadow`。
読んでみよう。

# ゴール

このコンテナで Falco ルール「Read sensitive file untrusted」を発火
させる。発火 = solve。

# 背景

Falco の最も基本的な検知ルール。Linux の `/etc/shadow` は本来 root
だけが読むファイル — 攻撃者が侵入後に最初に試す典型ターゲット。
Falco は **コンテナ内であっても host kernel の syscall** を観測して
いるので、コンテナ境界の中で読んでも逃れられない。

# 試すこと

  cat /etc/shadow

# クリア条件

`Read sensitive file untrusted` がユーザ Namespace で発火する。

# 環境にあるもの

  /opt/ctf/missions/02-credential-files/fixtures/welcome.txt  ← このファイル

────────────────────────────────────────────────────────────────────
HINT 1 (気付き): パスワードの実体はどこに眠るか
────────────────────────────────────────────────────────────────────

Linux で「パスワードハッシュの実体」が置かれる伝統的なファイルは?
本来 root しか読まないそのファイルこそ、侵入後の攻撃者が真っ先に
狙う場所。Falco はその読み取りを直接見ている。

────────────────────────────────────────────────────────────────────
HINT 2 (解答): 開けば発火する
────────────────────────────────────────────────────────────────────

  cat /etc/shadow
  # /etc/sudoers, /etc/pam.d/su なども対象

Falco が見るのは fd.name (開いたパス) なので、`cat` / `less` / `head`
どのコマンドで開いても発火する。

(次の Mission 03 では「同じ中身に別 path で到達して発火を避ける」)

参考: REFERENCE.md §3.1
