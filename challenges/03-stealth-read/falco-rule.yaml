challengeId: 03-stealth-read
type: evade
forbiddenRules:
  - Read sensitive file untrusted
# Placeholder for local dev/tests. Real flag injected at deploy via FLAGS_FILE
# (rendered from falco-ctf-platform events/<date>/flags.sops.yaml). Must equal
# the ctf-user chart's dev default so local end-to-end submit passes.
expectedFlag: FALCO{dev-stealth-read}
# ATT&CK mapping — canonical source for docs/Navigator layer (make gen-attack).
# T1006: evade twin of 02. Reaching /etc/shadow via /proc/self/root/... bypasses
# the path-string file monitor — accessing the same data through an alternate path
# to defeat file-access monitoring is Direct Volume Access. (T1622 Debugger Evasion
# is unrelated: this evades a Falco file rule, not a debugger.)
attack:
  tactic: "Defense Evasion"
  techniqueId: "T1006"
  techniqueName: "Direct Volume Access"
