challengeId: 03-stealth-read
type: evade
forbiddenRules:
  - Read sensitive file untrusted
# Placeholder for local dev/tests. Real flag injected at deploy via FLAGS_FILE
# (rendered from falco-ctf-platform events/<date>/flags.sops.yaml). Must equal
# the ctf-user chart's dev default so local end-to-end submit passes.
expectedFlag: FALCO{dev-stealth-read}
# ATT&CK mapping — canonical source for docs/Navigator layer (make gen-attack).
# T1006 (previous mapping) is Network Devices/Windows only on attack.mitre.org —
# no Linux platform, so it cannot describe this procfs technique. Checked Linux
# Hide Artifacts sub-techniques for a closer fit: T1564.001 (hidden files/dirs),
# T1564.005 (separate hidden filesystem), T1564.013 (bind-mount over /proc to
# hide a process from ps/top) — none model reading an existing file via an
# alternate path. Remapped to the T1564 parent (platform includes Linux):
# reaching /etc/shadow via /proc/self/root/... to stay off the path-string
# monitor's radar is "hiding an artifact of behavior to evade detection".
attack:
  tactic: "Defense Evasion"
  techniqueId: "T1564"
  techniqueName: "Hide Artifacts"
