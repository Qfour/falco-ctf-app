# GENERATED by gen-values.sh — DO NOT EDIT. Run `make gen-values`.
#
# All-missions mode (deploy-user.sh <user> all): the `plant` initContainer
# runs every evade mission's plant.sh in one seed script (ADR-0001 Option B).
# Flag values come from the ctf-flags Secret (CTF_FLAG_* keys), injected into
# `plant` only — never into the challenge container; no flag literals live
# here.
plant:
  seedScript:
    - sh
    - -c
    - |
      set -eu
      PLANT_SEED_ROOT=/plant-seed

      # ---- 03-stealth-read ----
      # seed /etc/shadow from the build-time snapshot (ADR-0001 S-a) — runs once
      mkdir -p "${PLANT_SEED_ROOT}/etc"
      cp -a "/opt/ctf/plant-seed/etc/shadow" "${PLANT_SEED_ROOT}/etc/shadow"
      #
      # Mission 03 (Stealth Read) — plant the flag at the end of /etc/shadow.
      #
      # ADR-0001 Option B: this script runs in the `plant` initContainer, never in
      # the challenge container. It never touches the real /etc/shadow — it writes
      # into the seed emptyDir at $PLANT_SEED_ROOT (gen-values.sh sets this var and,
      # because `plant-seed-source` above is declared, copies the build-time
      # snapshot from /opt/ctf/plant-seed/etc/shadow into
      # $PLANT_SEED_ROOT/etc/shadow *before* this line runs). The chart then binds
      # $PLANT_SEED_ROOT/etc/shadow onto /etc/shadow in the challenge container via
      # a subPath mount (read-only) — the participant sees the same path they
      # always did.
      #
      # CTF_FLAG_03_STEALTH_READ is injected into the `plant` initContainer from the
      # ctf-flags Secret (the chart supplies a FALCO{dev-...} default for local
      # runs).
      echo "# ${CTF_FLAG_03_STEALTH_READ:?flag env not set by ctf-user chart}" >> "${PLANT_SEED_ROOT}/etc/shadow"

      # ---- 05-silent-search ----
      #
      # Mission 05 (Silent Search) — embed the flag inside a fake OpenSSH private
      # key so the player must read it without putting "id_rsa" on the command
      # line.
      #
      # ADR-0001 Option B: this script runs in the `plant` initContainer and writes
      # into the seed emptyDir at $PLANT_SEED_ROOT/root/.ssh (not the real
      # /root/.ssh — the challenge image doesn't even have that directory). No
      # `plant-seed-source` header is declared because this plant-target has no
      # build-time base data to restore first (unlike /etc/shadow) — 05 creates the
      # whole file itself. The chart binds $PLANT_SEED_ROOT/root/.ssh onto
      # /root/.ssh in the challenge container via a subPath mount.
      #
      # CTF_FLAG_05_SILENT_SEARCH is injected into the `plant` initContainer from
      # the ctf-flags Secret.
      mkdir -p "${PLANT_SEED_ROOT}/root/.ssh"
      cat > "${PLANT_SEED_ROOT}/root/.ssh/id_rsa" <<EOF
      -----BEGIN OPENSSH PRIVATE KEY-----
      b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAA
      tzc2gtZWQyNTUxOQAAACBm${CTF_FLAG_05_SILENT_SEARCH:?flag env not set by ctf-user chart}_FAKEKEY=
      -----END OPENSSH PRIVATE KEY-----
      EOF
      chmod 600 "${PLANT_SEED_ROOT}/root/.ssh/id_rsa"

      # ---- 10-final-exfil ----
      #
      # Mission 10 (Final Exfil) — plant the master key as a CTF_MASTER_KEY line at
      # the end of /etc/shadow. Reading it naively fires Read-sensitive (evade with
      # Mission 03 /proc/self/root). The capstone then requires exfiltrating it to
      # the collector over HTTP (see requireExfil in falco-rule.yaml).
      #
      # ADR-0001 Option B: same seed-dir model as 03-stealth-read/plant.sh (which
      # shares this plant-target — gen-values.sh dedupes the /opt/ctf/plant-seed/
      # snapshot copy to run once, then appends both missions' lines in mission-id
      # sort order). See that file's header for the full explanation.
      #
      # CTF_FLAG_10_FINAL_EXFIL is injected into the `plant` initContainer from the
      # ctf-flags Secret.
      echo "# CTF_MASTER_KEY: ${CTF_FLAG_10_FINAL_EXFIL:?flag env not set by ctf-user chart}" >> "${PLANT_SEED_ROOT}/etc/shadow"
  mounts:
    - /etc/shadow
    - /root/.ssh
