apiVersion: v2
name: collector
description: Falco CTF collector — participant-facing front; forwards submit/me/display-name and the boss exfil drop to the scoreboard (P11.5)
type: application
version: 0.1.0
# appVersion is set at publish time to the falco-ctf-app git SHA (Invariant I4/I5).
appVersion: "0.0.0"
# P21 item 5 (REFACTORING.md): shared securityContext / default-deny
# NetworkPolicy / labels / image named templates (step1 landed the first two,
# step2 added labels+image). Local sibling path — run `helm dependency build`
# after any change to charts/falco-ctf-common (see Makefile `lint`/`deploy-local`).
dependencies:
  - name: falco-ctf-common
    version: "0.1.0"
    repository: "file://../falco-ctf-common"
