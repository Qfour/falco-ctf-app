apiVersion: v2
name: docs
description: Falco CTF challenge documentation site (MkDocs Material + per-mission PDF)
type: application
version: 0.1.0
# appVersion is set at publish time to the falco-ctf-app git SHA (Invariant I4/I5).
appVersion: "0.0.0"
