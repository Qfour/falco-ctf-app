apiVersion: v1
kind: Service
metadata:
  name: docs
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "docs.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "docs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
